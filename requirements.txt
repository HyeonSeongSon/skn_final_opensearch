# FastAPI 웹 프레임워크
fastapi==0.104.1
uvicorn[standard]==0.24.0

# 데이터 검증 및 직렬화
pydantic==2.5.0

# OpenSearch 클라이언트
opensearch-py==2.4.2

# 임베딩 및 리랭크 모델 (호환성 버전)
sentence-transformers>=2.2.2,<3.0.0
transformers>=4.40.0
torch>=1.9.0
huggingface-hub>=0.19.0
FlagEmbedding>=1.2.0

# 기본 라이브러리
numpy>=1.21.0,<2.0.0
python-dotenv==1.0.0

# HTTP 요청 라이브러리 (테스트 클라이언트용)
requests>=2.25.0

# 선택적 의존성 (성능 향상)
python-multipart==0.0.6  # 파일 업로드 지원

# 로깅 및 모니터링 (선택사항)
# prometheus-client==0.19.0 